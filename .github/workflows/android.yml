name: Android APK Build

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout latest source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Show current files
        run: |
          pwd
          ls -la
          echo "===== gradle.properties ====="
          cat gradle.properties || true
          echo "===== app/build.gradle ====="
          cat app/build.gradle
          echo "===== MainActivity.kt AndroidX check ====="
          grep -R "androidx" app/src/main || true

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 8.10.2

      - name: Build APK
        run: gradle --no-daemon clean :app:assembleDebug -Dandroid.useAndroidX=true -Dandroid.enableJetifier=true

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: HTDI-MES-APK
          path: app/build/outputs/apk/debug/app-debug.apk
